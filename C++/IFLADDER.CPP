#include<iostream.h>
void main()
{
char ch;
cout<<"enter the character:";
cin>>ch;
if(ch>=48&&ch<=57)
cout<<"itis a digit";
elseif(ch>=65&&ch<=90)
cout<<"it is a uppercase letter";
elseif(ch>=97&&ch<=122)
cout<<"it is a lowercase letter":
}