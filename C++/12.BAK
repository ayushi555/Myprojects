#include<conio.h>
#include<iostream.h>

float operate(int a[],int x);
void main()
{
int i,c;
int ch[55];
cout<<"enter size:";
cin>>c;
cout<<"enter the array;;";
for(i=0;i<=c;i++)
{
cin>>ch[i];
}

operate(ch,c);
for(i=0;i<=c;i++)
cout<<"the no.s are"<<ch[i]<<endl;
}

float operate(int a[],int x)
{
int i;

for(i=0;i<=x;i++)
{
if(a[i]%2==0)
a[i]=a[i]*1/2;
else
a[i]=a[i]*2;
}
return 0;}


